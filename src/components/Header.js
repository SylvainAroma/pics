import React, { Component } from "react";

export default class Header extends Component {
  render() {
    return (
      <div>
        <header className="ui header">Sylvain's image search</header>
      </div>
    );
  }
}
